name: Snake generator

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *" # run every hour

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create snake
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg
            dist/github-snake-dark.svg?palette=github-dark
            dist/ocean.gif?color_snake=orange&color_dots=#bfd6f6,#8dbdff,#64a1f4,#4b91f1,#3c7dd9

      - name: Modify SVG for theme
        run: |
          # Add CSS to SVG for theme support
          echo '<style>
          :root {
            --color-square-light: #ebedf0;
            --color-square-dark:  #161b22;
            --color-corner-light: rgba(27, 31, 35, 0.06);
            --color-corner-dark: black;
          }
          .light-theme .square {
            fill: var(--color-square-light);
            stroke: var(--color-corner-light);
          }
          .dark-theme .square {
            fill: var(--color-square-dark);
            stroke: var(--color-corner-dark);
          }
          </style>' | tee -a dist/github-snake.svg dist/github-snake-dark.svg

          # Wrap SVG content in a div for theme classes
          sed -i '1s/^/<div class="light-theme">\n/' dist/github-snake.svg
          sed -i -e '$a<\/div>' dist/github-snake.svg
          sed -i '1s/^/<div class="dark-theme">\n/' dist/github-snake-dark.svg
          sed -i -e '$a<\/div>' dist/github-snake-dark.svg

      - name: Push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
